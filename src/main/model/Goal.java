package model;

import java.util.Date;

// A log entry representing a strength target
public class Goal extends LogEntry {
    public Goal(Date date, String name) {
        super(date, name);
    }
}
